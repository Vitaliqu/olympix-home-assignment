# Etherscan API — needed only for --pool mode (auditing live on-chain contracts)
# Not required for local file audits: npx ts-node cli/scaling-audit.ts --file src/...
ETHERSCAN_API_KEY=YOUR_ETHERSCAN_KEY
